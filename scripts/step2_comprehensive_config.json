{
  "data_types": {
    "analyze_gp_data": true,
    "analyze_clean_data": true,
    "analyze_noisy_data": true,
    "comment": "Which data types to analyze: GP (raw), clean (processed), noisy"
  },
  
  "dimension_estimation": {
    "methods": ["PCA", "k-NN", "TwoNN", "MLE"],
    "max_points_for_analysis": 2000,
    "k_neighbors_range": [10, 20, 30],
    "comment": "Dimension estimation settings. All methods will be applied to each data type."
  },
  
  "curvature_analysis": {
    "enable_mean_curvature": true,
    "enable_pca_curvature": true,
    "k_neighbors": 40,
    "max_points_for_curvature": 1000,
    "weight": "gaussian",
    "regularization": 1e-10,
    "fallback_on_failure": true,
    "min_neighbors": 10,
    "comment": "Curvature estimation with improved NaN handling through fallback methods."
  },
  
  "geometric_statistics": {
    "compute_diameter": true,
    "compute_volume": true,
    "compute_density_analysis": true,
    "sample_size_for_diameter": 2000,
    "comment": "Geometric property computations for each data type."
  },
  
  "output": {
    "save_detailed_results": true,
    "save_per_dataset": true,
    "save_summary_tables": true,
    "comment": "Output options: detailed JSON, per-dataset files, summary CSV tables."
  },
  
  "performance": {
    "max_datasets": null,
    "show_progress": true,
    "comment": "Performance settings. Set max_datasets to limit for testing."
  }
}
